apiVersion: batch/v1
kind: Job
metadata:
  name: metadata-kafka-topic-init-job
  namespace: {{ .Release.Namespace }}
spec:
  completions: 1
  parallelism: 1
  backoffLimit: 1
  ttlSecondsAfterFinished: 1800
  template:
    spec:
      restartPolicy: Never
      initContainers:
      - name: wait-for-kafka
        {{- if .Values.global.registryPrefix }}
        image: {{ .Values.global.registryPrefix }}/docker.io/library/busybox:1.35.0
        {{- else }}
        image: docker.io/library/busybox:1.35.0
        {{- end }}
        command: ['sh', '-c', '
          until nc -z -w 2 {{ .Release.Name }}-kafka.{{ .Release.Namespace }}.svc.cluster.local 9092;
          do
            echo "Waiting for Kafka to be ready...";
            sleep 2;
          done;
          ']
      containers:
      - name: kafka-client
        {{- if .Values.global.registryPrefix }}
        image: {{ .Values.global.registryPrefix }}/docker.io/bitnami/kafka:3.7.1-debian-12-r4
        {{- else }}
        image: docker.io/bitnami/kafka:3.7.1-debian-12-r4
        {{- end }}
        env:
          - name: KAFKA_BOOTSTRAP_SERVERS
            value: "{{ .Release.Name }}-kafka.{{ .Release.Namespace }}.svc.cluster.local:9092"
          - name: OPERATION
            value: "create"
          - name: TOPIC_NAME
            value: "ingest-to-es"
          - name: PARTITIONS
            value: "8"
          - name: RETENTION_MS
            value: "21600000"
          - name: REPLICATION_FACTOR
            value: {{ .Values.kafka.replicationFactor | quote }}
        command: ["/bin/bash", "-c"]
        args:
          - |
            apt-get update && apt-get install -y jq
            
            case $OPERATION in
              create)
                echo "Creating topic $TOPIC_NAME"
                kafka-topics.sh --create \
                  --bootstrap-server $KAFKA_BOOTSTRAP_SERVERS \
                  --topic $TOPIC_NAME \
                  --partitions $PARTITIONS \
                  --replication-factor $REPLICATION_FACTOR \
                  --config retention.ms=$RETENTION_MS
                ;;

              *)
                echo "Invalid operation: $OPERATION"
                exit 1
                ;;
            esac