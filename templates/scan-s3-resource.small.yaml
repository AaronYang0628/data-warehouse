apiVersion: org.zhejianglab.astro.metadata/v1
kind: FlinkIngestTask
metadata:
  name: ingest-table-or-fits-from-s3-small
spec:
  paths: 
    - "s3://csst/test"
  allowedSuffixes:
    - fits
  platform: s3
  extraEnvs:
    esHost: "metadata-test-elasticsearch.metadata-test.svc.cluster.local"
    esPort: 9200
    datasetIndex: "datasetindex"
  extraSecret: 
    name: munge-oss-ingestion-credential
    namespace: metadata-test
    accessKeyName: access-key
    secretKeyName: secret-key
    endpointKeyName: s3-endpoint